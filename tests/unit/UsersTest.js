describe('Users', () => {
  // xit for ignore tests for now
  xit('is an ActiveOrm class', () => {

  });

  describe('find()', () => {
    xit('returns a single result', () => {

    });

    context('when it does not have records', () => {
      xit('returns empty', () => {

      });
    });
  });
});
